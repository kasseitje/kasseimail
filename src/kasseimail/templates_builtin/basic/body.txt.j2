{#- The plain-text body. See subject.j2 for the fields you can use here.

    Keep it: it is what you read in the dry run, and it is the body that goes out if you ever
    delete body.html.j2. Note that Graph sends one body or the other, not both -- when the HTML
    file exists, this text is written to the output directory but not into the message. -#}
Dear {{ first_name }},

This is the starter template. Replace this text with yours, in this file and in body.html.j2.

Kind regards,
